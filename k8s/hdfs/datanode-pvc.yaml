apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: datanode-pvc-0
  labels:
    app: hdfs
    role: datanode
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
  volumeName: datanode-pv-0

