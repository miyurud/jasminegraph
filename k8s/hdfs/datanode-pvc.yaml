apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: datanode-pvc-0
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
  volumeName: datanode-pv-0

